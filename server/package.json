{
  "name": "crmt-historico-contabil-financeiro-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Backend mínimo para integração Open Finance via Pluggy — só existe porque o Client Secret nunca pode ir para o navegador.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "pluggy-sdk": "^0.90.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.6",
    "@types/node": "^26.5.1",
    "tsx": "^4.23.13",
    "typescript": "~7.0.2"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
