{
  "name": "crmt-historico-contabil-financeiro-sync-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Servidor mínimo de sincronização do banco .sqlite entre seus próprios dispositivos — sem nenhum serviço de nuvem, roda inteiro na sua máquina/rede. Independente do server/ (que só existe para a integração Pluggy).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^24.10.9",
    "tsx": "^4.19.2",
    "typescript": "~5.9.3"
  }
}
